---
title: Nuxt Hooks
description: Hook into the Nuxt OG Image build-time and runtime behavior.
relatedPages:
  - path: /docs/og-image/nitro-api/nitro-hooks
    title: Nitro Hooks
  - path: /docs/og-image/api/config
    title: Nuxt Config
  - path: /docs/og-image/guides/community-templates
    title: Community Templates
---

## Build-time Hooks

### `nuxt-og-image:runtime-config`

**Type:** `(config: OgImageRuntimeConfig) => HookResult`{lang="ts"}

Modify the Nuxt OG Image runtime config at build-time.

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  hooks: {
    'nuxt-og-image:runtime-config': (config) => {
      config.colorPreference = 'dark'
    }
  }
})
```

### `nuxt-og-image:components`

**Type:** `(ctx: { components: OgImageComponent[], detectedRenderers: Set<RendererType> }) => HookResult`{lang="ts"}

Programmatically modify the components used by the module.

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  hooks: {
    'nuxt-og-image:components': (ctx) => {
      ctx.components = ctx.components.filter(c => c.category !== 'community')
    }
  }
})
```

## Runtime Hooks

Nitro hooks for runtime OG image generation are documented in the [Nitro API](/docs/og-image/nitro-api/nitro-hooks) section.
